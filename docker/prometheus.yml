# Конфигурация Prometheus: собираем метрики с API-сервиса.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: smart-moderator
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
